export const FIELD_OR_RENDER_VALIDATION_MESSAGE = `A 'field' or 'render function' is required for the component.pass at least one property to the component"`;
export enum EnumAction {
  ADD = 'add',
  DELETE = 'delete',
  UPDATE = 'update',
}
